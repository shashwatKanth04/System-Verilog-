# SystemVerilog – Dynamic & Associative Arrays

SystemVerilog programs demonstrating dynamic arrays (runtime-sizable) and 
associative arrays (indexed by arbitrary key types), including array methods.

| File | Description |
|---|---|
| [dynamic array file] | Dynamic array declaration, `new[]` allocation, resizing |
| [associative array file] | Associative array declaration, indexing by key |
| [array methods file] | Array methods — size(), delete(), exists(), first()/next() |

**Concept covered:** SystemVerilog dynamic arrays and associative arrays
**Status:** Lab exercises from PGCP-VLSI SystemVerilog module.
